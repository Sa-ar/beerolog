{
  "name": "beerolog",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "pnpm lint:js && pnpm lint:api",
    "typecheck": "turbo typecheck",
    "test": "pnpm test:api && pnpm contracts:check",
    "db:generate": "turbo db:generate",
    "db:migrate": "turbo db:migrate",
    "lint:js": "turbo lint",
    "lint:api": "uv --directory apps/api run ruff check . && uv --directory apps/api run ruff format --check .",
    "test:api": "uv --directory apps/api run pytest tests/ -v",
    "openapi:export": "uv --directory apps/api run python scripts/export_openapi.py",
    "contracts:generate": "pnpm openapi:export && pnpm --dir apps/web run api:generate",
    "contracts:check": "pnpm contracts:generate && git diff --exit-code -- apps/api/openapi.json apps/web/src/lib/api-client/schema.d.ts",
    "sync:api": "uv --directory apps/api sync --extra dev",
    "dev:api": "pnpm sync:api && uv --directory apps/api run uvicorn app.main:app --reload --host 127.0.0.1 --port 8000"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.0.0",
    "eslint": "^10.4.0",
    "eslint-plugin-jsx-a11y": "^6.10.2",
    "globals": "^17.6.0",
    "prettier": "^3.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.59.4"
  },
  "packageManager": "pnpm@11.3.0"
}
