# @beerolog/icons

Shared taste-profile icon types, purpose resolution, SVG sanitization, and the `GeneratedTasteIcon` React component.

Purpose-key rules mirror `beerolog-icon-service` (Python) for parity.
